node_modules/
dist/
*.local

# TypeScript / build emit
*.tsbuildinfo
vite.config.js
vite.config.d.ts

.env
.env.*
!.env.example

# Design exports (reference only — fetched on demand, not committed)
*.tar.gz
design-system/
woordkast-design/

# Editor / OS
.DS_Store
.vscode/

# Design canvas build output — regenerated by design/canvas/build.mjs and the
# canvas seeder from the *.body.html sources (see design/canvas/README.md).
design/canvas/*.dc.html
design/canvas/woordkast-design-system.html
